name: Hello MLflow

conda_env: src/config/conda_env.yaml

entry_points:
  main:
    parameters:
      algorithm: {type: str, default: 'RandomForestRegressor'}
      grid_search: {type: bool, default: False}
    command: "python trainer.py {algorithm} {grid_search}"