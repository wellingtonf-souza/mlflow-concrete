name: Concrete MLflow

conda_env: src/config/conda_env.yaml

entry_points:
  main:
    parameters:
      algorithm: {type: str, default: 'RandomForestRegressor'}
      grid_search: {type: str, default: 'n'}
    command: "python trainer.py --algorithm={algorithm} --grid_search={grid_search}"